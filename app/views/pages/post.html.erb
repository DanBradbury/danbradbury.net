<header class="align--center">
<span><%= @post.created_at.strftime("%B %d, %Y") %></span>

<h2><%= @post.title %></h2>
</header>

<%= GitHub::Markup.render_s(GitHub::Markups::MARKUP_MARKDOWN, @post.content).html_safe %>

<%= render "footer" %>
